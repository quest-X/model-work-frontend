# TODO

跨仓库总交接见 [model-work-node/TODO.md](https://github.com/quest-X/model-work-node/blob/main/TODO.md)。本仓记录 Frontend 尚未完成的工作。

## 现场待办（2026-09-24，等待 Tailscale）

以下对应总 TODO 的 LAN 编号；等用户回家并确认接入后再恢复，不自动部署。
本轮山东专用预览/炉次界面整合到 `main`，不整支合入通用 `master`；
功能源码基线 `939b0dd8`，预览/轮询/运行器 64 项、3 suites 与专项 ESLint 通过。

- [ ] **LAN-1/2｜现场清单与按需部署**：核对实际页面资源、部署提交和并行部署；仅在确实缺少修复且授权明确时，复用 `scripts/deploy-shangang-frontend.sh` 固定提交构建与回滚。`DRY_RUN` 仍会 SSH，不能作为离网检查。
- [ ] **LAN-3｜炉次浏览器闭环**：真实登录后核对炉次列表、等级、时间、事件详情和钻取，验证日期/时区/跨日/刷新一致性与权限拒绝，不以接口成功代替页面验收。
- [ ] **LAN-4a｜历史录像慢加载**：回到原问题录像，分别记录列表刷新、首字节、持续传输、首帧，验证拖动、重复打开及并发快照/历史刷新，不持续卡在 0%。
- [ ] **LAN-4b/c｜实时预览与协议目录**：先验 DLK-04 LL-HLS 冷启动/重复打开/持续出帧、两个目录重试入口、协议切换释放、隐藏/返回恢复、多标签页，再回归 DLK-02/03；其他浏览器协议及 Tailscale 下 RTSP/SRT 实际入口单列核对。
- [ ] **LAN-5｜执行器页面与业务连续性**：对照实际 AIPACK-07 状态、已授权 `system.wait` 的成功退出记录和程序运行器显示，确认检测/录像及帧数正常；旧 1.1.2 故障不代表当前状态。
- [ ] **LAN-6/7｜稳定性与恢复**：按约定窗口验证长时间播放、断网恢复、多用户、跨浏览器及获授权后的启动恢复；未经授权不主动断网、重启或压测生产。

## 未完成任务

- [x] **P0｜真实浏览器业务闭环**：已在当前本机运行时完成登录、控制/生产平台加载、测试图片导入、YOLOv8n 真实推理、6 个检测框预览和完整 YOLO 训练集导出；ZIP 已通过内容与完整性检查。
- [ ] **P0｜账户跨设备验收**：HTTPS Cookie、刷新恢复、第二设备、退出、退出其他设备、权限撤销、授权过期、重放拒绝及真实限流已验收；独立改密复验由用户豁免。仅保留当前记住密码会话到 2026-10-08 16:50:41 +08:00 的自然过期实时时钟验收。
- [ ] **P0｜一键 OTA 页面实机验收**：当前页面已实测加载六节点，并在控制端 0 个发布包时禁用版本选择和批次创建。`.16` 的旧 1.0.7 源对候选 Windows 节点不可解析，且获授权上海节点均已是 1.0.7；需先确定新可信 HTTPS 源及低版本测试节点或更高正式版本，再验收完整批次状态。
- [ ] **P1｜v1.0.8 多群界面现场验收**：有权限现场群的查询、切换、CLI 入群申请与浏览器审批、退出、图谱刷新和中央群隐藏源码已完成；当前仅剩真实现场联合验收。
  - [x] 群选择后读取 `/groups/{group_id}`，按现场群快照展示对应 Main/Node 成员并拒绝错群响应。
  - [x] 已接入按群资源图、群切换和刷新，并隔离中央群与现场群图谱状态。
  - [x] 已接入节点 CLI 入群申请、Master 浏览器审批、群号轮换和保留现场数据的退出确认；浏览器手工登记入口已按正式流程移除。
  - [x] 普通成员逐群授权源码已接入，按群详情、成员和资源范围回归通过。
  - [ ] 已登录浏览器和至少两个真实现场群的联合验收仍待现场执行。
- [ ] **P1｜真实节点状态视觉验收**：源码已统一“正常 / 故障”；仍须用在线、短暂离线、持续失败和恢复节点检查列表、地图、图谱与汇总一致性。
- [ ] **P1｜电文完整历史读取与字段校验（2026-09-22）**：独立“电文”页已上线，但当前仍读取程序事件摘要，受节点日志尾部 `64 KB / 100 条`限制，不代表完整 `ixcom.jsonl` 历史。后续复用现有 Artifact Range 接口按文件尾部加载并支持“加载更早”，保留全部原始字段和坏行提示；以页面有效条数、首尾 `frame_idx`、时间和字段与原文件一致为完成条件。DLK 写入字段统一及现场程序重启须另获维护授权。
- [ ] **规划池｜设备管理器（2026-09-15）**：计划在“拓展引擎”菜单的“计算群”下方增加“设备管理器”；本次仅记录名称与入口位置，不进行产品设计或代码实现，待后续明确管理对象和操作范围后再立项。
- [x] **P2｜最终前端质量门**：2026-09-21 商业版本机验证 125 个测试套件、800 项测试全部通过；全仓 TypeScript 0 错误、ESLint 0 错误/0 警告，生产构建通过；真实 Chrome WebCrypto → Python Node 签名联调通过，并确认篡改凭据被拒绝。

## 当前同步基线

- 分支：`main`
- GitHub 同步：本地 `main` 与 `origin/main` 对齐；精确提交以 `git rev-parse HEAD` 为准。
- 本机的 `dist-*` 目录是未跟踪构建产物，不属于 GitHub 源码交接。
